# Quick Note 进度记录

这个文件用于跨对话恢复进度。每完成一个阶段，必须更新本文件。

## 当前状态

- 当前阶段：阶段 6，Rust 与 Tauri 后端边界
- 当前目标：理解 Tauri 的前后端边界，实现可接收参数并返回错误的 Rust command
- 当前分支：main
- GitHub 仓库地址：https://github.com/sleepinrain/quick_note
- 最近一次有效 commit：1d11069 feat: add note search

## 已完成阶段

- 阶段 1：GitHub 与项目管理
- 阶段 2：环境准备与项目初始化
- 阶段 3：TypeScript 与 React 主窗口
- 阶段 4：SQLite 本地持久化
- 阶段 5：标签与搜索

## 当前阶段待办

- [ ] 阅读并理解现有 `src-tauri` 结构
- [ ] 实现返回应用信息的 Rust command
- [ ] 实现接收标签参数的 Rust command
- [ ] 使用 `Result` 返回并处理错误
- [ ] 记录前端与 Rust 后端边界决策
- [ ] 完成并推送阶段 6

## 技术决策记录

### 2026-05-31：确定学习与开发路线

- 使用 Tauri 2 + React + TypeScript + SQLite。
- 先做普通主窗口，再做桌面特性。
- GitHub 工作流放在第一阶段。
- 每阶段必须有验收标准和 commit。
- MVP 阶段不做云同步、AI、复杂动画、富文本编辑器。

## 重要项目约定

- 阶段文档目录：`docs/learning-plan/`
- 总入口：`docs/learning-plan/00-路线总览.md`
- 当前进度：`docs/learning-plan/99-进度记录.md`
- 协作方式：Codex 给出具体步骤、命令、代码和检查方式；用户在本机执行并反馈结果。
- 新对话开始后，先恢复当前阶段，不要重新规划整条路线。
- 每阶段至少一次 commit。
- commit message 使用英文，格式尽量清楚，例如：
  - `docs: add learning plan`
  - `chore: initialize tauri react app`
  - `feat: add in-memory note editor`
  - `feat: persist notes with sqlite`
  - `test: add parser unit tests`

## 新对话恢复提示词

复制下面这段给 Codex：

```text
请先阅读 docs/learning-plan/00-路线总览.md、docs/learning-plan/99-进度记录.md，以及当前阶段对应的阶段文档。然后根据记录继续带我做 quick_note 项目。不要从头规划，先恢复进度。
```

## 阶段完成记录模板

每完成一个阶段，在下面追加记录。

```text
## 阶段 X 完成记录

- 完成日期：
- 完成内容：
- 关键文件：
- Git commit：
- GitHub issue / PR：
- 遇到的问题：
- 暂缓处理的问题：
- 下一阶段：
```

## 风险和待确认事项

- 是否已经安装 Git：已确认（2026-05-31：用户终端显示 `git version 2.54.0.windows.1`；Codex 当前进程 PATH 尚未刷新）
- 是否已经安装 Node.js LTS：已确认，Node v24.16.0，npm 11.13.0
- 是否已经安装 Rust：已确认，rustc 1.96.0，cargo 1.96.0，工具链 `stable-x86_64-pc-windows-msvc`
- 是否已经有 GitHub 仓库：已确认，https://github.com/sleepinrain/quick_note
- 是否要使用 npm 作为包管理器：默认 npm
- 是否需要英文 README：默认先中文，后续再补英文
- Windows Tauri 依赖：已确认 Visual Studio 2022 Community C++ 工具和 WebView2 Runtime。
- 阶段 2 验证：`npm.cmd run build` 已通过；`cargo check` 已通过；`npm.cmd run tauri dev` 已打开 Quick Note 桌面窗口；`npm.cmd install` 第一次因 npm registry 连接重置失败，重试后成功。
- 阶段 2 注意事项：根目录旧草稿 `开发路径.md`、`技术文档.md` 未纳入 Git，脚手架 `--force` 初始化后已不在工作区；后续以 `docs/learning-plan/` 作为正式计划来源。
- Tauri 仓库说明：`src-tauri/` 中的 Rust 源码、配置、图标和 `Cargo.lock` 应提交；`src-tauri/target/` 是本地编译缓存，当前约 4.9GB，已被 `.gitignore` 忽略，不应上传到 GitHub。
- 阶段 3 当前验证：`npm.cmd run build` 已通过；内存版数据刷新或重启后丢失是本阶段预期行为。
- 阶段 4 当前进展：已接入 Tauri SQL 插件并初始化数据库表；已实现从 SQLite 加载笔记和新建笔记，重启应用后笔记记录仍然存在。相关提交：`a875a92`、`9dbce08`、`beb3d71`。
- 阶段 5 验证：标签规范化和 SQLite 持久化已通过；重启后标签仍然存在；空搜索、`#tag` 搜索、标题和正文关键词搜索均已通过。
- 阶段 5 搜索语法：空字符串显示最近更新笔记；`#work` 按规范化标签精确搜索；普通文本使用 SQLite `LIKE` 搜索标题和正文。

## 阶段 1 完成记录

- 完成日期：2026-05-31
- 完成内容：初始化本地 Git 仓库，创建 `README.md` 和 `.gitignore`，提交学习计划文档，创建 GitHub 仓库并推送 `main` 分支，创建阶段 2 到阶段 8 的 GitHub issues。
- 关键文件：`README.md`、`.gitignore`、`docs/learning-plan/`
- Git commit：1766ea6 `docs: add learning plan`
- GitHub issue / PR：
  - https://github.com/sleepinrain/quick_note/issues/1
  - https://github.com/sleepinrain/quick_note/issues/2
  - https://github.com/sleepinrain/quick_note/issues/3
  - https://github.com/sleepinrain/quick_note/issues/4
  - https://github.com/sleepinrain/quick_note/issues/5
  - https://github.com/sleepinrain/quick_note/issues/6
  - https://github.com/sleepinrain/quick_note/issues/7
- 遇到的问题：首次误在 `C:\WINDOWS\System32` 执行 `git init`；SSH 推送因未配置 GitHub SSH key 失败，后续改用 HTTPS 推送成功。
- 暂缓处理的问题：GitHub SSH key 配置暂缓，后续需要时再处理。
- 下一阶段：阶段 2，环境准备与项目初始化。

## 阶段 2 完成记录

- 完成日期：2026-05-31
- 完成内容：确认 Node.js/npm、Rust MSVC 工具链、Visual Studio C++ 工具和 WebView2 Runtime；使用 `create-tauri-app` 初始化 Tauri 2 + React + TypeScript 项目；更新应用名称、README 和基础占位界面；安装依赖并验证桌面开发模式。
- 关键文件：`package.json`、`package-lock.json`、`src/`、`src-tauri/`、`README.md`
- Git commit：59f2463 `chore: initialize tauri react app`
- GitHub issue / PR：https://github.com/sleepinrain/quick_note/issues/1
- 遇到的问题：首次 `npm.cmd install` 因 npm registry 连接重置失败，重试后成功；PowerShell 执行策略会拦截 `npm.ps1`，使用 `npm.cmd` 可正常执行。
- 暂缓处理的问题：根目录旧草稿 `开发路径.md`、`技术文档.md` 未纳入 Git，脚手架 `--force` 初始化后已不在工作区；后续以 `docs/learning-plan/` 作为正式计划来源。
- 下一阶段：阶段 3，TypeScript 与 React 主窗口。

## 阶段 3 完成记录

- 完成日期：2026-06-03
- 完成内容：实现内存版主窗口，包含笔记列表、编辑器、标签输入、空状态、新建、编辑、切换和删除。
- 关键文件：`src/types/note.ts`、`src/pages/MainWindow.tsx`、`src/components/`、`src/App.tsx`、`src/App.css`
- Git commit：9c716be `feat: add in-memory note editor`
- GitHub issue / PR：https://github.com/sleepinrain/quick_note/issues/2
- 遇到的问题：暂无。
- 暂缓处理的问题：数据仍保存在 React 内存中，刷新或重启后丢失；阶段 4 接入 SQLite。
- 下一阶段：阶段 4，SQLite 本地持久化。

## 阶段 4 完成记录

- 完成日期：2026-07-21
- 完成内容：接入 Tauri SQL 插件，初始化 SQLite 表结构，实现笔记加载、新建、编辑和删除持久化，并验证重启后数据仍然存在。
- 关键文件：`src/db/client.ts`、`src/db/schema.ts`、`src/db/notes.ts`、`src/pages/MainWindow.tsx`、`src/components/NoteEditor.tsx`、`src-tauri/capabilities/default.json`
- Git commit：80b84f8 `feat: persist note deletion with sqlite`（阶段 4 最后一个功能提交）
- GitHub issue / PR：https://github.com/sleepinrain/quick_note/issues/3
- 数据库位置：`C:\Users\Sakura\AppData\Roaming\com.sleepinrain.quicknote\quick_note.db`
- 遇到的问题：`sql:default` 不包含 SQL 写入权限，需要额外配置 `sql:allow-execute`。
- 验收结果：笔记的新建、编辑和删除均已持久化；关闭并重新启动应用后结果保持不变。
- 暂缓处理的问题：标签关系和搜索功能留到阶段 5。
- 下一阶段：阶段 5，标签与搜索。

## 阶段 5 完成记录

- 完成日期：2026-07-23
- 完成内容：实现标签规范化、标签关系持久化、标签重启恢复、搜索词解析、标签精确搜索以及标题和正文关键词搜索。
- 关键文件：`src/tags/normalizeTags.ts`、`src/components/TagInput.tsx`、`src/db/tags.ts`、`src/db/notes.ts`、`src/search/parseQuery.ts`、`src/pages/MainWindow.tsx`、`src/App.css`
- Git commit：`457ad60`、`5669bee`、`3e90044`、`8ac61e9`、`1d11069`
- GitHub issue / PR：https://github.com/sleepinrain/quick_note/issues/4
- 标签规则：按逗号分割，去除前后空格和开头的 `#`，转为小写，忽略空标签并去重。
- 搜索语法：空字符串显示最近更新；`#tag` 按标签精确搜索；普通文本使用 `LIKE` 搜索标题和正文。
- 遇到的问题：输入过程中立即规范化会删除刚输入的逗号；改为保留输入框原始文本，并在失焦时规范化和保存。
- 暂缓处理的问题：Tauri SQL JavaScript API 没有事务接口，目前标签关系按顺序更新；FTS5 和复杂组合搜索留到后续升级。
- 验收结果：多个标签可正常保存，重启后仍然存在；标签搜索、关键词搜索、空搜索和无结果搜索均符合预期。
- 下一阶段：阶段 6，Rust 与 Tauri 后端边界。
